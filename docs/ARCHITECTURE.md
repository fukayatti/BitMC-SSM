# Bit-MC-SSM: CPU-Native 1.58-bit Memory-Cached State Space Model
**〜 GPU・小数の行列演算依存からの完全脱却を目指す次世代CPU特化型LLMアーキテクチャ 〜**

---

## 📌 1. エグゼクティブサマリー (Executive Summary)

### 1.1 解決する課題
従来のLLM（Transformer系）推論および訓練は、以下のハードウェア的ボトルネックを抱えています。
1. **Memory Bandwidth Wall (メモリ帯域の壁):** トークン生成ごとに巨大な重み（FP16）と際限なく肥大化する **KV Cache ($O(L^2)$)** をDRAMから読み出す必要があり、GPUの演算器（FLOPS）がVRAM待ちで遊休化する。
2. **高価なGPUハードウェア依存:** 浮動小数点積和演算（FP16/FP8 GEMM）に特化したTensor Coreが必須となり、エッジデバイスや安価なCPUサーバーでの高速駆動が困難。
3. **純粋SSM（Mamba等）の限界:** 固定長隠れ状態 ($O(L)$) は軽量である反面、長い系列で過去の情報を忘却し、連想記憶（Associative Recall）やNeedle-in-a-Haystackなどの検索タスクでTransformerに劣る。
4. **低ビット量子化における活性化の外れ値 (Activation Outliers):** 8-bit / 4-bit 活性化量子化において、特定のチャネルに巨大な外れ値が集中して量子化誤差が増大する。
5. **訓練時の損失計算 VRAM 圧迫:** 50,000語以上の大規模語彙において、Logitsテンソルが 1 ステップあたりギガバイト単位の VRAM を消費し、バッチサイズが制限される。

### 1.2 コアコンセプト
**「BitNet v2 ($\mathcal{H}$-BitLinear + FWHT による外れ値抑制)」** × **「1.58-bit (Ternary) 重み ＋ INT4 活性化の乗算ゼロ演算」** × **「Delta-SSM (Fast/Slow 双対減衰) ＋ Memory Caching」** × **「Triton GPU SRAM 融合訓練カーネル」** × **「T-MAC (LUT参照型 Zero-GEMM C++ 推論)」** を統合し、**GPU 訓練スループット 30,000+ tok/s、CPU 推論速度 100+ tokens/s を両立する超低消費電力・高効率LLM** を実現しました。

---

## 🏗️ 2. 全体アーキテクチャ構成図

```
[入力：離散トークン ID]
       │
       ▼
【 1. INT8/INT4 Embedding 層 】 ──── ルックアップテーブル（LUT配列参照）による整数ベクトル変換
       │
       ▼
【 2. H-BitLinear 層 (BitNet v2) 】 ─ 高速アダマール変換 (FWHT) で外れ値分散 ＋ INT4 活性化量子化
       │                               重み {-1, 0, +1} × INT4 活性化による乗算回数ゼロ（加減算・LUT参照）
       ▼
【 3. Delta-SSM (双対減衰) 】 ─────── Fast/Slow 2系統の時定数によるマルチスケール記憶更新
       │
       ├────────────────────────► [ セグメント末尾で State を Checkpoint Pool へ保存 (数KB) ]
       │                                                                │
       │   [ 現在のトークン Query ] ────► 【 Sparse Router 】 ◄───────────┘
       │                                          │ (Top-k 選択: k=2)
       ▼                                          ▼
【 4. Gated Aggregation 】 ◄────────────── [ Top-k Cached States ]
       │ (オンライン状態と過去チェックポイントの加算合成)
       ▼
【 5. SwiGLU FFN (BitNet v2) 】 ──── 2系統射影 ＋ SiLU ゲート ＋ H-BitLinear 出力射影
       │
       ▼
【 6. Bit-Unembedding (LM Head) 】 ── T-MAC 並列 LUT 行列ベクトル積による次トークン Logits 出力
       │
       ▼
[出力：次トークン ID (Greedy / Top-k Sampling)]
```

---

## ⚙️ 3. 6大コア技術仕様

### 3.1 演算系: BitNet v2 ($\mathcal{H}$-BitLinear) ＆ INT4 活性化
* **重み量子化 ($W$):** $\{-1, 0, +1\}$ の3値（1.58ビット）。2ビット（4重み/1Byte）でメモリに超高密度パッキング。
* **高速アダマール変換 (FWHT: Fast Walsh-Hadamard Transform):**
  * 活性化を直交行列 $H_d$ で回転させることで、尖度（Kurtosis）を **269 $\to$ 2.01** へ激減。
  * 外れ値を全次元に均一拡散させ、精度の劣化なく **INT4 (4-bit) 活性化量子化** を実現。
  * 変換自体もバタフライ構造の「加算・減算のみ（乗算ゼロ）」で $O(d \log d)$ で高速計算。
* **計算原理:**
  $$Y = \gamma \cdot \left( W_{\text{ternary}} \cdot \text{Quantize4bit}(H_d X) \right)$$
  浮動小数点の乗算器を一切使わず、CPUの加算・減算・LUT参照のみで完結。

### 3.2 局所処理系: Delta-SSM (Fast/Slow 双対時定数)
* **課題解決:** 単一の減衰係数では「直前の短期文脈」と「数千トークン前の長期文脈」を両立できない。
* **双対状態更新式:**
  $$h_{t,\text{fast}} = \sigma(\text{decay}_{\text{fast}}) \odot h_{t-1,\text{fast}} + B_t u_t$$
  $$h_{t,\text{slow}} = \sigma(\text{decay}_{\text{slow}}) \odot h_{t-1,\text{slow}} + B_t u_t$$
  $$h_t = \frac{1}{2} (h_{t,\text{fast}} + h_{t,\text{slow}})$$
  $$y_t = (C_t h_t + u_{\text{conv}, t}) \odot \text{SiLU}(\text{Gate}_t)$$
* 高速な短期応答と緩やかな長期記憶を極小の演算量で同時に獲得。

### 3.3 長期記憶系: Sparse Selective Memory Caching (MC-SSC)
* **セグメント分割:** 入力系列をサイズ $C = 32 \sim 256$ のセグメントに分割。
* **チェックポイント保持:** 各セグメントの末尾状態のみをチェックポイントバッファに保存（4096トークンでもわずか数KB）。
* **疎ルーティング (Sparse Selective Retrieval):**
  1. 各セグメントの代表キーベクトル $K_{\text{seg}}^{(s)}$ を保持。
  2. 現在トークンの Query $q_t$ とのコサイン類似度で Top-$k$（$k=2$）個のチェックポイントのみを抽出。
* **出力集約 (Gated Aggregation):** 純粋SSMの「忘却」を防ぎ、Transformer同等の長期想起能力（Needle-in-a-Haystack耐性）を維持。

### 3.4 最適化系: GaLore (Gradient Low-Rank Projection)
* **CPU/GPU省メモリ学習:** 重み勾配 $G \in \mathbb{R}^{m \times n}$ を低ランク射影行列 $P \in \mathbb{R}^{m \times r}$ ($r \ll \min(m,n)$) を用いて低次元化。
* 30M〜300M規模のモデル学習において、オプティマイザ状態（Adam states）のメモリ消費量を **65〜80% 削減** し、低リソース環境での高速収束を実現。

### 3.5 推論加速系: T-MAC (LUT参照型 Zero-GEMM Kernel)
* **テーブル参照化:** 4要素の入力 chunk $(x_0, x_1, x_2, x_3)$ に対する三値重み（256通りの全組み合わせ）を L1/L2 キャッシュ上の LUT に事前展開。
* **$O(1)$ テーブルルックアップ:** 行列ベクトル積を加算ループから「キャッシュメモリ上の値の読み出し」へと変換し、CPU SIMD（AVX2 / AVX-512）のパイプラインスループットを極限まで向上。

### 3.6 訓練加速系: Triton GPU Fused Kernel Suite (`python/triton_kernels/`)
* **Flash Cross-Entropy:** 50,257 語彙のロジットテンソルを VRAM に置かず、SRAM 内でオンライン LogSumExp & 逆伝播を完結。メモリ消費を 99.8% 削減。
* **Fused FWHT + INT4 STE:** GPU レジスタ内でアダマールバタフライ演算と 4-bit 量子化を一括処理。
* **Fused Delta-SSM Parallel Scan:** $O(L^2)$ の減衰テンソルを作らず $O(L)$ レジスタ走査。
* **Fused RMSNorm & SiLU Gating:** メモリ往復回数を極小化し、Dual T4 GPU で 30,000+ tokens/s の高スループットを達成。

---

## 📊 4. 計算複雑度とメモリ階層の比較

| 指標 | 従来の Transformer (FP16) | 純粋な SSM / Mamba (FP16) | **Bit-MC-SSM (本アーキテクチャ)** |
| :--- | :--- | :--- | :--- |
| **計算量 (Time Complexity)** | $O(L^2)$ | $O(L)$ | **$O(L \cdot k) \approx O(L)$** ($k \ll C$) |
| **KV/状態メモリ消費** | $O(L)$ (4K文脈で数百MB〜GB) | $O(1)$ (数MB) | **$O(L/C)$ (4K文脈で数十〜数百KB)** |
| **1パラメータあたりの容量** | 16 bit (2.0 Byte) | 16 bit (2.0 Byte) | **1.58 bit (0.2 Byte) $\to$ 1/10 (2-bit packing)** |
| **主たる推論演算** | FP16/FP8 GEMM (積和演算) | FP16 GEMM ＋ ODE連続計算 | **乗算ゼロ: INT4 加減算 ＋ FWHT ＋ T-MAC LUT** |
| **外れ値抑制** | なし (量子化誤差大) | なし | **Fast Walsh-Hadamard Transform (Kurtosis 2.01)** |
| **訓練時損失計算** | $O(B \cdot L \cdot V)$ VRAM 圧迫 | $O(B \cdot L \cdot V)$ | **Flash Cross-Entropy (SRAM オンライン LSE)** |
| **メモリ配置先** | GPU VRAM / DRAM (低速) | GPU VRAM / DRAM | **CPU L1/L2/L3 キャッシュ (TB/s級)** |
| **実測推論速度 (30M)** | CPU: 1〜5 tokens/s | CPU: 5〜15 tokens/s | **CPU: 103.4 tokens/s (9.67 ms/tok)** |

---

## 🗺️ 5. 開発フェーズ達成状況 (Milestones)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Phase 1: PyTorch プロトタイプ & CPUベースライン実証 (15M〜30M 規模)  【完了】 │
│  ・TinyStories による小規模言語モデル構築                                   │
│  ・STE (Straight-Through Estimator) ＋ 三値量子化 {-1, 0, +1} の動作実証     │
│  ・Shift-SSM ＋ Memory Caching (MC-SSC) の有効性確認                        │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Phase 2: BitNet v2 (H-BitLinear) ＋ GaLore ＋ Delta-SSM 統合        【完了】 │
│  ・FWHT (高速アダマール変換) による活性化外れ値抑制 (Kurtosis 269 → 2.01)   │
│  ・GaLore (低ランク射影勾配) によるオプティマイザ省メモリ化 (メモリ 70% 削減)│
│  ・Delta-SSM (Fast/Slow 双対時定数) によるマルチスケール学習基盤確立        │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Phase 3: 2-bit バイナリエクスポーター & ヘッダー自動検出            【完了】 │
│  ・学習済み {-1, 0, +1} 重みを 2-bit パッキング形式バイナリへ変換 (容量 1/8) │
│  ・Scaleup モデル用マジックヘッダー (BSSM/BITS) 自動判別ローダー実装        │
│  ・語彙 (GPT-2 BPE 50,257語) マッピングパーサーの実装                       │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Phase 4: C++20 / T-MAC SIMD 高速推論エンジン構築 (GPU完全脱却)      【完了】 │
│  ・PyTorch / CUDA 一切非依存の完全スタンドアロン C++20 推論バイナリ         │
│  ・T-MAC L1-Cache LUT 参照カーネル ＋ AVX2 SIMD ベクトル化                  │
│  ・【実測達成】 30Mモデルで 103.4 tokens/s (9.67 ms/tok) のリアルタイム推論 │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Phase 5: クラウドスケールアップ学習 (Colab GPU) & 自動デプロイ      【完了】 │
│  ・Colab GPU (T4/A100) 上で 30M〜300M モデルの高速学習ノートブック提供      │
│  ・学習完了時の自動 2-bit エクスポート ＆ ローカル CPU C++ 推論連携         │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Phase 6: Triton GPU カーネル群による極限訓練最適化 (Dual T4 DDP)    【完了】 │
│  ・Flash Cross-Entropy、Fused FWHT+INT4、Fused RMSNorm/SiLU の完全実装     │
│  ・2x T4 DDP 環境で 30,000+ tokens/s (3〜4倍速) の爆速スループット達成      │
│  ・全 17 件の単体テスト & 勾配パリティ検証合格                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## ⚡ 6. 実測ベンチマーク (30M Parameter Model)

* **ハードウェア環境:** 汎用 x86_64 CPU (AVX2 SIMD, 4スレッド)
* **モデル構成:**
  * パラメータ数: **30,248,832 (約3,000万パラメータ)**
  * レイヤー数: **8層**
  * 隠れ層次元 ($d_{\text{model}}$): **384**
  * SSM 状態次元 ($d_{\text{state}}$): **32**
  * 語彙サイズ: **50,257 (GPT-2 BPE)**
* **推論結果:**

| 項目 | 計測結果 | 備考 |
| :--- | :--- | :--- |
| **生成速度** | **103.41 tokens / 秒** | GPU・CUDA 一切不使用 |
| **トークン生成遅延** | **9.67 ms (9,670 μs)** | 人間の読書速度（約200語/分 $\approx$ 5 tok/s）の20倍以上 |
| **メモリ消費量** | **約 78 MB** | メインメモリ・L3キャッシュ常駐可能 |
| **テキスト品質** | **文法・文脈・会話構造ともに極めて良好** | TinyStories データセットで検証 |

---

## 🎯 7. 到達目標とユースケース

1. **完全エッジ・スタンドアロン駆動:** Raspberry Pi、スマートフォン、車載SoC上でGPU・NPUを使わず、CPUコアのみで実用速度（数十〜100+ tokens/s）のテキスト生成。
2. **オンメモリ常駐型ローカルエージェント:** 30M〜300Mモデルがメインメモリを圧迫せず、L3キャッシュ/省電力DRAMに常駐してバッテリーを消費しないバックグラウンド推論。
3. **インフラコストの破壊:** 高価なGPUインスタンスを廃止し、安価な汎用CPUクラウドインスタンスでの大規模スケールアウト。
